{
  "name": "historia-hardsim",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build:lib": "tsc -p tsconfig.build.json",
    "check": "npm run typecheck && npm test && npm run build:lib",
    "calibration:worldbank": "node scripts/fetch-worldbank-civil-data.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
